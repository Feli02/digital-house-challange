<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/profileStyle.css">
    <%- include('./headPartial')%>

    <title>Digital Movies: Perfil</title>
</head>
<body>
    <header class="header">
            <picture class="picture-cont">
                <a href="/"><img src="/images/logo_transparent.png" alt="logo"></a>
            </picture>
    </header>
    <main>
        <h1>Perfil</h1>

        <div class="perfil">
            <p>Nombre de User: <%= user.name%></p>
            <p>Email de User: <%= user.email%></p>
            <div class="admin">
                <p>Es administrador: </p>
                <% if(user.rol != 0){ %>
                    <p>Si</p>
                <% } else {%>
                    <p>No</p>
                <% } %></p>
            </div>
            
        </div>


        <!-- Editar user -->
        <a href="/usuario/editar/<%= user.id %>">Editar</a>


        <!-- logout -->
        <form action="/usuario/salir" method="get">
            <p><button type="submit">Cerrar Sesión</button></p>
        </form>


        <!-- delete -->
        <button type="button" id="deleteAccount">Eliminar Cuenta</button>

        <section id="confirmDelete">
            <form action="/usuario/eliminar/<%= user.id %>" id="formDelete" method="POST">
                <p>¿Estás seguro que quieres eliminar tu cuenta?</p>

                <button type="button" id="cancel">Cancelar</button>
                <button type="submit" >Eliminar</button>
            </form>
        </section>
    </main>

    <script src="/js/deleteConfirm.js"></script>
</body>
</html>