<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <%- include('./headPartial')%> 

    <title>Digital Movies: Detalle ID<%= user.id%></title>
</head>
<body>
    <%- include('./header')%> 
<main>
    <h1>Detalles de User <%= user.id%></h1>    
    

    <p>Nombre: <%= user.name %></p>
    <p>Email: <%= user.email %></p>
    <div class="admin">
        <p>Rol: </p>
        <% if(user.rol != 0){ %>
            <p>Administrador</p>
        <% } else {%>
            <p>Normal user</p>
        <% } %></p>
    </div>

  

    
    <a href="/allUsuarios/edit/<%= user.id %>">Editar</a>
    
    <form method="POST" action="/allUsuarios/delete/:id/<%= user.id %>">
    
        <button type="submit">
            Eliminar
        </button>
    
    </form>
    
    
</main>
</body>
</html>